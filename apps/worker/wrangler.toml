name = "ai-reseller-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Note: For local development, secrets are loaded from ../../.env.local
# For production, use: wrangler secret put SECRET_NAME

[vars]
# All variables (including MARKUP_PERCENTAGE) are now loaded from ../../.env.local
# This keeps all configuration in one place

[build]
command = "npm run build"

[dev]
port = 8787
ip = "0.0.0.0"
